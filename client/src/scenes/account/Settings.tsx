import * as React from 'react';
import './styles/settings.css';

const SettingsPage = () => (
  <section className="settings">
    <aside className="settings__aside">options</aside>
  </section>
);

export default SettingsPage;
