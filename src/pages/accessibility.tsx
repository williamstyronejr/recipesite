import Head from 'next/head';

const AccessibilityPage = () => (
  <section className="accessbility">
    <Head>
      <title>Accessibility - Reshipi Bukku</title>
    </Head>

    <header className="accessbility__header" />
  </section>
);

export default AccessibilityPage;
